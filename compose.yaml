services:
  backend:
    build: ./backend
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - ./backend/.env
    environment:
      DATA_DIR: /app/data
    volumes:
      - analysis_data:/app/data
    restart: unless-stopped

volumes:
  analysis_data:
