# Browser API address. The full website uses the backend on the same origin.
# VITE_* values are public: never place the model key or application token here.
VITE_API_BASE_URL=/api/v1
# Vite development proxy for /api and /health. Ignored by the production build.
API_PROXY_TARGET=http://127.0.0.1:8000
